import React from 'react';

const SliderTemplates = (props) => {
    console.log(props);

    return (
        <div>
            slider templates
        </div>
    );
};

export default SliderTemplates;
